// import { takeLatest, put, call, select, takeEvery } from 'redux-saga/effects';
// import {decrease, increase,countSelectors, ItemCount } from '../count';
// import { useSelector } from 'react-redux';


// export function* handleIncrease(action: any) { 
//     yield put(increase(action));

// }
// export function* handleDecrease(action: any) { 
//     yield put(decrease(action));

// }
// // Watcher saga
// export function* countSaga() {
//   // Listen for the loginUser action and call the handleLogin saga when dispatched
//   yield takeLatest(increase.type, handleIncrease);
//   yield takeLatest(decrease.type, handleDecrease);

// }

// export default countSaga;